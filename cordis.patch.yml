# DSH Marketplace bundle patch
# Registers the marketplace as a DSH profile layer with proper plugin registration.
- insert:
    - id: marketplace
      name: 'dsh-marketplace'
      config:
        distIndex: !!js require.resolve("dsh-marketplace/dist/index.html")
